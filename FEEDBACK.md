# Feedback to the CALL-E team (running log)

Observations from building voxprobe's CALL-E adapter, with enough detail to reproduce. Times are PDT unless noted; numbers
masked. Constructive by intent: this is what a developer sees from the outside.

| # | When | What we did | What we saw | Why it matters / suggestion |
|---|---|---|---|---|
| 1 | 2026-08-21 18:26, 18:30, 18:31 | `POST /v1/calls` via `calle-ai` 0.7.0 (`calls.create`), US recipient, task 0.4–2.5 kB, `result_schema` plain object, `Idempotency-Key` set | **503 `provider_unavailable`**, message "The call plan could not be prepared.", `details: {}`, no `request_id`. Read-only `GET /v1/goals` 200 with the same key. Same signature as public issue awesome-phone-call-agents#213 | A 503 with an empty `details` and no request id gives the caller nothing to act on or quote in a report. Suggest: populate `details.reason` (planner vs telephony vs region), return a `request_id`, and surface incidents on a status page / `GET /v1/status` |
| 2 | 2026-08-21 | `CalleAPIError.__str__` returns only the message; `code`/`status_code`/`details` are attributes | Our CLI initially swallowed the code ("The call plan could not be prepared.") | SDK ergonomics: include code + status in `__str__` so logs carry them by default |
| 3 | 2026-08-21 | `https://test-api.heycall-e.com` with a live `iams_live_` key | 401 "Invalid or missing API key" | Docs mention the test environment but not how to obtain a test key; a $0 sandbox that returns synthetic CallTasks would let integrators run CI without spending calls |
| 4 | 2026-08-21 | Read the OpenAPI 0.6.0 / SDK | `transcript_turns.offset_seconds` is an integer (or null); no audio via API | Integer offsets cannot support turn-taking/latency measurement; audio (even mono) or ms offsets would make post-call QA possible from the API alone |
| 5 | 2026-08-21 18:55–19:05 | Followed the agent install guide: `npx skills add … --skill calle -g` (fails: "PromptScript does not support global skill installation"; per-project install works), `npm i -g @call-e/cli`, brokered login, `calle mcp tools` OK. Then `calle mcp call plan_call` (plan only, no dial) for our number and for a fictional +1 202 555 number | Both return the upstream error verbatim: **`status_code: 429, model_name: gpt-5.5 … 'You have no credits remaining. Add credits to continue using the API at https://platform.openai.com/…' code: credit_balance_exhausted`**. The CLI wrapper (`calle call plan --json`) hides this as `plan_call_error` with `status_code: null`. Timing matches the REST 503 `provider_unavailable` ("call plan could not be prepared") | Root cause of today's outage appears to be the planner's upstream LLM quota, not telephony. Suggestions: (a) a status page / incident banner in the dashboard; (b) map upstream quota exhaustion to a distinct, honest error (`planner_unavailable`, retryable=true) on both REST and MCP; (c) don't leak the upstream vendor's billing message to end users; (d) have the CLI surface the server's error text (it is already in `result.content[0].text`) |
| 6 | 2026-08-21 | `calle call plan` with a 2.3 kB goal (our full persona task) vs a 1-line goal | Same error for both, so no evidence of a length limit yet — will re-test when planning is back | Publish the goal/task size limits in docs |
| 7 | 2026-08-22 01:32:32Z → 01:32:40Z | Same `POST /v1/calls` retried with the same idempotency key after ~1 h | **Accepted (201, `status: queued`) and failed 8 s later**: `failure_code: provider_unavailable`, `failure_message: "The call plan could not be prepared."`, recipient `pending`, `attempts: []`, `GET /v1/calls/{id}/events` → 0 events. MCP `plan_call` at the same minute still returns the upstream `429 credit_balance_exhausted` | The failure mode changed from synchronous 503 to accepted-then-failed, which moves the cost of the outage onto the integrator (polling, webhooks) and raises a billing question: does an accepted task that never dialed count as a "billable call"? Suggest documenting that, emitting at least one developer event on terminal failure, and failing synchronously when the planner is known-down |
| 8 | 2026-09-07 19:55–20:20 | First account (created Aug 21): dashboard login works; regenerated API key | Every REST endpoint → **403 `forbidden` "This account is not allowed to access CALL-E"** (GET /v1/goals, GET /v1/calls/{id}); MCP token for the same account also invalidated ("A usable CALL-E auth token is required") although cached until 2029. A freshly created account works immediately | An account can be silently locked out of the Developer API while the dashboard stays green; the error text doesn't say why or who to contact. Suggest: dashboard banner + reason code (suspended / needs verification / region) and a support link in the 403 body |
| 9 | 2026-09-08 03:29Z (call_rzLC…) | Real call to our receptionist line; compared `transcript_turns` with the stereo recording's audio-derived transcript | Our receptionist said "8 AM on Saturday **with** Doctor Chen"; CALL-E's turn reads "**without** doctor Chen" and its `confirmed.provider` = "another provider, not Doctor Chen". Also two of our turns are absent from `transcript_turns` (a PHI-leak line CALL-E barged over, and a cut-off confirmation) | Callee-side ASR errors flow straight into `structured_result` with no uncertainty marker. Suggest: per-turn ASR confidence in `transcript_turns`, and quoting the callee turn that supports each `evidence[]` item (turn index) so integrators can audit |
| 10 | 2026-09-08 (call_9OaE…, saturday-false-offer row) | Receptionist (planted bug) offers Saturday at a Mon–Fri clinic; the task never states the clinic's hours | `task_completed: true`, `completion_confidence 0.92 "high"`, booking day "Saturday" reported faithfully | Confidence measures agreement between the transcript and the report, not world-truth — a downstream automation would act on an impossible booking with 0.92 confidence. Suggest: document that `completion_confidence` is report-consistency, not correctness; offer an optional `known_facts`/`constraints` field in the task contract that the agent must reconcile against |
| 11 | 2026-09-08 | 6 real calls via `calls.create` + `wait_for_result` | Every call completed normally; end-to-end 80–110 s; our line answered on the first ring each time; no `recipient_blocked`/AMD issues on a "this call may be recorded" greeting | Positive: outbound to an AI-answered VoIP number just works |

